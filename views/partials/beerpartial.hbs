{{#if this.image_url}}
  <img
    id='random-this-picture'
    src='{{this.image_url}}'
    alt='{{this.name}} bottle'
  />
{{/if}}
<h2>{{this.name}}</h2>
<p>{{this.tagline}}</p>
<p>{{this.description}}</p>
